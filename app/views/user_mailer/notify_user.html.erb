<%= @msg.try(:text).try(:html_safe) %>
<%#= render partial: "/admin/high_scores/top_10_in_all_games", locals: {user: @user} #%>

<br>
<br>
<%= link_to "Unsubscribe", unsubscribe_path(@user.try(:password)) if @user.present? %>
